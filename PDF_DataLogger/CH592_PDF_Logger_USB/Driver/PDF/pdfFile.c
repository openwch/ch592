#include "string.h"
#include "pdfFile.h"
#include "SW_UDISK.h"
#include "Internal_Flash.h"
#include "CHRV3UFI.h"
#include "usbfs_device.h"
#include "spi_flash.h"
#include "peripheral.h"
#include "usb_connect.h"

//#define CONFIG_PDFFILE_DEBUG
#ifdef CONFIG_PDFFILE_DEBUG
#define LOG_INFO(...)           PRINT(__VA_ARGS__)
#else
#define LOG_INFO(...)
#endif

uint32_t data_len;
uint32_t data_tollen;

//__attribute__ ((aligned(4)))  uint8_t data_buf[PDF_TMP_BUF_LEN_MAX+PDF_TMP_BUF_LEN_EXT];

const uint8_t sec0[] ={
    ,
};

// SEC4/6
const uint8_t  BPB_Media[] =
{
    0xf8,0xff,0xff,0xff,0xff,0xff
};


__HIGH_CODE
void pdf_memcpy( void *pDst, const void *pSrc, uint32_t len )
{
    uint8_t *dst;
    const uint8_t *src;
    if( pDst == 0 || pSrc == 0 || len == 0 ){
        return;
    }

    src = (uint8_t *)pSrc;
    dst = (uint8_t *)pDst;
    do{
        *dst++ = *src++;
    }while( --len );
}


void udisk_enable( void )
{
#if  FUN_UDISK
    /* USBOTG_FS device init */
    USBFS_Device_Init( );
#endif
}


void udisk_format( void )
{
    uint32_t addr = 0;

    for( int sec=0;sec<40;sec++ )
    {
        memset(pDISK_FAT_BUF, 0, DEF_UDISK_SECTOR_SIZE);
        if( sec == 0 ) {
            pdf_memcpy( pDISK_FAT_BUF,sec0,sizeof(sec0) );
        }
        else if( sec == 4 || sec == 6 ) {
            pdf_memcpy( pDISK_FAT_BUF,BPB_Media,sizeof(BPB_Media) );
        }
        W25XXX_WR_512B(pDISK_FAT_BUF, addr, 512);
//        IFlash_Prog_512( addr, (uint32_t*)pDISK_FAT_BUF);
        addr += DEF_UDISK_SECTOR_SIZE;
    }
}


/*********************************************************************
 * @fn      mStopIfError
 *
 * @brief   Checking the operation status, displaying the error code and stopping if there is an error
 *          input : iError - Error code input
 *
 * @return  none
 */
#if FUN_FILE_CREATE
static void mStopIfError( uint8_t iError )
{
    if ( iError == ERR_SUCCESS )
    {
        /* operation success, return */
        return;
    }
    /* Display the errors */
    LOG_INFO( "Error:%02x\r\n", iError );
    /* After encountering an error, you should analyze the error code and CH103DiskReday status, for example,
     * call CH103DiskReday to check whether the current USB disk is connected or not,
     * if the disk is disconnected then wait for the disk to be plugged in again and operate again,
     * Suggested steps to follow after an error:
     *     1,call CH103DiskReday once, if successful, then continue the operation, such as Open, Read/Write, etc.
     *     2,If CH103DiskReday is not successful, then the operation will be forced to start from the beginning.
     */
    while(1);
}
#endif


void open_file( char *filename )
{
#if FUN_FILE_CREATE
    uint8_t s;

    LOG_INFO( "Open\r\n" );
    strcpy( mCmdParam.Open.mPathName,filename );
    s = CHRV3FileOpen( );                         //open file
    LOG_INFO("open file. s=%x\n",s);
    if( s == 0x91 )                               //disk is not formatted
    {
        udisk_format( );
        s = ERR_MISS_FILE;
    }

    if ( s == ERR_MISS_DIR || s == ERR_MISS_FILE ) //file not found on disk
    {
        LOG_INFO( "Create new file\n" );
        strcpy( mCmdParam.Create.mPathName, filename ); //File name, create a file in the root directory
        s = CHRV3FileCreate( ); //Create a new file and open it, if the file already exists, delete it first and then create a new one
        mStopIfError( s );
    }
    else //file found on disk
    {
        udisk_enable(); //USB peripheral initialization, simulate U disk
        while(1)
        {
            if(USB_CONNECT_CHECK() == 0)
            {
                DelayMs(10);
                SYS_ResetExecute();
            }
        }
    }
#endif

    /* Storage Data Count Clear */
    data_len = 0;
    data_tollen = 0;
}


void write_file( uint8_t *pData )
{
#if FUN_FILE_CREATE
    uint8_t s;

    mCmdParam.Write.mSectorCount = 1;       //写入所有扇区的数据
    mCmdParam.Write.mDataBuffer = pData;    //指向文件数据缓冲区的起始地址
    s = CHRV3FileWrite( );                  //向文件写入数据
    mStopIfError( s );
#endif
}


void close_file( void )
{
    LOG_INFO( "Close\r\n" );
#if FUN_FILE_CREATE
    uint8_t s;
    uint32_t file_len;

    if( data_len )
    {
        write_file( RAM_BUFFER.PDF_BUFFER.pdf_data_buf );
        s = CHRV3FileQuery( );                                      // Query current file information
        mStopIfError( s );
        file_len = CHRV3vFileSize - CHRV3vSectorSize + data_len;
        LOG_INFO( "Modify\r\n" );
        mCmdParam.Modify.mFileAttr = ATTR_READ_ONLY;                // Input parameter: new file attribute, if it is 0FFH, it will not be modified
        mCmdParam.Modify.mFileTime = MAKE_FILE_TIME( 8,8,8 );       // Input parameters: the new file time, if it is 0FFFFH, it will not be modified, and the default time generated by the new file will be used
        mCmdParam.Modify.mFileDate = MAKE_FILE_DATE( 2023, 8, 1 );  // Input parameters: New file date: 2015.05.18
        mCmdParam.Modify.mFileSize = file_len;                      // Input parameters: new file length, the length of the file written in bytes should be automatically updated by the program library when the file is closed, so it is not modified here
        s = CHRV3FileModify( );                                     // Modify the information of the current file, the modification date
        mStopIfError( s );
    }
    mCmdParam.Close.mUpdateLen = 0;
    s = CHRV3FileClose( );
    mStopIfError( s );

    /* file created */
    udisk_enable( );  //USB peripheral initialization, simulate U disk
#endif
}


uint32_t pdf_data_proces( void *buf, uint32_t length )
{
    if( length > PDF_TMP_BUF_LEN_EXT ){
        LOG_INFO("max length error.len= %d > %d\n",length,PDF_TMP_BUF_LEN_MAX);
        return 1;
    }

    if( data_tollen + length > PDF_FILE_MAX_LEN ){
        LOG_INFO( "file error len=%d\r\n",data_tollen + length );
        return 2;
    }

    pdf_memcpy( &RAM_BUFFER.PDF_BUFFER.pdf_data_buf[data_len], buf, length );
    data_len += length;

    if( data_len >= PDF_TMP_BUF_LEN_MAX ){
        data_len -= PDF_TMP_BUF_LEN_MAX;
        write_file( RAM_BUFFER.PDF_BUFFER.pdf_data_buf );
        if( data_len ){
            pdf_memcpy( RAM_BUFFER.PDF_BUFFER.pdf_data_buf,&RAM_BUFFER.PDF_BUFFER.pdf_data_buf[PDF_TMP_BUF_LEN_MAX],data_len);
        }
    }
    data_tollen += length;

    return 0;
}
